const statsHTML = `<h2 id="username">Username</h2>
<h4>0th place</h4>
<h4>Score: 3500</h4>`

const updateIntervalID = setInterval(updateStats, 500);

let test = 0;

function updateStats() {
    //test = test + 1;
    //document.getElementById("username").textContent = test;
}